# dev

Start the Angular development server.

## Steps

Run the following command from the project root (`task-manager/`):

```bash
npm start
```

This runs `ng serve` and opens the app at **http://localhost:4200** with hot module reload enabled.

## Build for production

```bash
npm run build
```

Output is in `dist/task-manager/`.
